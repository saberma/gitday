---
applications:
  .:
    name: github-friend
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    url: ${name}.${target-base}
    mem: 256M
    instances: 2
    services:
      postgresql-github-friend:
        type: postgresql
      redis-github-friend:
        type: redis
